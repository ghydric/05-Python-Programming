# Lambda functions
# Temporary one liner anonymous (cannot be called again) functions

# single argument
square = lambda x : x*x
print(square(5))

# multiple arguments
sum = lambda x, y, z : x + y + z
print(sum(5, 10, 15))